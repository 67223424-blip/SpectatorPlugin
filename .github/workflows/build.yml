name: Build Spectator Plugin

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
        
    - name: Build with Maven
      run: mvn clean package -DskipTests
      
    - name: Upload JAR
      uses: actions/upload-artifact@v4
      with:
        name: SpectatorPlugin
        path: target/*.jar


**อัพเดท Actions เป็นเวอร์ชั่นล่าสุด** (v4)

---

### วิธีที่ 4: ตรวจสอบไฟล์ทั้งหมด

ในหน้า Repository ของคุณ ต้องมีโครงสร้างแบบนี้:
```
SpectatorPlugin/
├── .github/
│   └── workflows/
│       └── build.yml ✅
├── src/
│   └── main/
│       ├── java/
│       │   └── com/
│       │       └── spectator/
│       │           └── plugin/
│       │               └── SpectatorPlugin.java ✅
│       └── resources/
│           └── plugin.yml ✅
├── pom.xml ✅
└── README.md
